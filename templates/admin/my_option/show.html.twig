{% extends 'base.html.twig' %}

{% block title %}MyOption{% endblock %}

{% block body %}
    <div class = "container mt-4">
        <h1>MyOption</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ my_option.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ my_option.name }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('my_option_index') }}">back to list</a>

        <a href="{{ path('my_option_edit', {'id': my_option.id}) }}">edit</a>

        {{ include('admin/my_option/_delete_form.html.twig') }}
    </div>
{% endblock %}
